import { DimensionValue } from "react-native/types"

const EIGHTY: DimensionValue = "80%"
const NINETY: DimensionValue = "90%"
const HUNDRED: DimensionValue = "100%"

export const FrameSizes = {
  normal: EIGHTY,
  wide: NINETY,
  full: HUNDRED,
}